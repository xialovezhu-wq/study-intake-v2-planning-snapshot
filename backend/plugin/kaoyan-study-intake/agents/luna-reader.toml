model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "read-only"
approval_policy = "never"

[agents]
enabled = false

[study_intake]
role = "reader"
formal_write_allowed = false
cross_subject_access = false
one_branch_per_session = true
one_mcp_launcher_per_session = true
